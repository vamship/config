'use strict';

/**
 * Utilites for initializing and accessing namespace scoped application
 * configuration.
 */
module.exports = {
    /**
     * Config component
     */
    config: require('./config')
};
